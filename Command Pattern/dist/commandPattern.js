"use strict";
const form = document.querySelector('form');
form === null || form === void 0 ? void 0 : form.addEventListener('submit', (e) => {
    e.preventDefault();
});
function myFunction() {
    console.log('works');
}
class Database {
}
